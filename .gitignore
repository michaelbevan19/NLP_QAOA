# Python
__pycache__/
*.pyc

# Virtual environments (machine-specific -- see HANDOFF.md Sec 3/5, both
# the Mac's .venv/.pytools and this machine's Windows .venv are excluded)
.venv/
.pytools/
venv/

# Results / generated output (large-ish, regenerated by main.py / report.py,
# not source -- commit specific results files deliberately if you want them
# versioned, e.g. `git add -f results/full_results.json`)
results/
*.png
all_outputs.txt

# OS cruft
.DS_Store
Thumbs.db
